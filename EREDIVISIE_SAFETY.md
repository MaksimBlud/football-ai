# Eredivisie safety contract

Eredivisie is research-only and MARKET_ONLY. Structural V2 remains `CALIBRATION_REQUIRED` with no alpha or edge threshold. Production model artifacts are not loaded, trained, overwritten, staged, or promoted by any Eredivisie workflow. Durable prediction observations are persisted only from the serialized market-shadow boundary; conflicts are fatal.
