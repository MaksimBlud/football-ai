name: RPL Operational Bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RPL bootstrap already completed successfully; retained only as an explicit manual record."
